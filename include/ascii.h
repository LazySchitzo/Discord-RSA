#ifndef ascii
#define ascii

// Convert and print out a string as ASII values.
void stringToASCII(char * input);

// Get the sum of all values of an array.
int sum(char * input);

// Get the sum of all values of an array minus one value. Not sure if there is a term for this.
int nSum(char * input);

#endif
